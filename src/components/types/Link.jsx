export default function Link() {
  <>
    {/* {data.description && <p>{data.description}</p>}
    <a
      href={data.url}
      target="_blank"
      rel="noopener noreferrer"
      style={{
        margin: "0",
        maxWidth: "100%",
        padding: "0 10px",
        textAlign: "justify",
      }}
    >
      {data.title}
    </a> */}
  </>;
}
