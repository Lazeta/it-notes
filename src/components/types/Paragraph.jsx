export default function Paragraph() {
  <>
    {/* <p
      style={{
        margin: "0",
        maxWidth: "100%",
        padding: "0 10px",
        textAlign: "justify",
      }}
    >
      {data.paragraph.split("\n").map((line, index) => (
        <span key={index}>
          {line}
          <br />
        </span>
      ))}
    </p>; */}
  </>;
}
